from django.contrib import admin
from .models import Doc

# @admin.register(Page)
# class PageAdmin(admin.ModelAdmin):
#     list_display = ('title', 'owner', 'created_at', 'updated_at')
#     search_fields = ('title', 'owner__username')  # Enable search
